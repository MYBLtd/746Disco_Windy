[Unit]
Description=Windy display – HTTP file server (serves windy.bin on port 8080)
After=network.target

[Service]
Type=simple
WorkingDirectory=/var/lib/windy
ExecStart=/usr/bin/python3 -m http.server 8080
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
